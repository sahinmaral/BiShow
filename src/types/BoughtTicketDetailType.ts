import { ActivitySeanceType } from "./ActivitiyTicketType";
import Activity from "./Activity";

type BoughtTicketDetailType = {
  activity:Activity;
  seance:ActivitySeanceType;
};

export default BoughtTicketDetailType;
